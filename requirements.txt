# Everything the maintenance tooling in tools/ and the build scripts need.
#   pip install -r requirements.txt
rdflib>=7.0
openpyxl>=3.1

# Optional: validating assurance-case data against shapes/ (see shapes/README.md)
# pyshacl>=0.25
